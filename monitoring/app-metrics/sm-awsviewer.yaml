apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: awsviewer
  namespace: monitoring
  labels:
    Release: kube-prometheus-stack
spec:
  namespaceSelector:
    matchNames:
      - default
  selector:
    matchLabels:
      app: awsviewer
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
