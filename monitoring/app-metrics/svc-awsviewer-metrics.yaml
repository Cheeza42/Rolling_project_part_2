apiVersion: v1
kind: Service
metadata:
  name: awsviewer-metrics
  namespace: dev
  labels:
    app: awsviewer
spec:
  selector:
    app: awsviewer
  ports:
    - name: metrics
      port: 80
      targetPort: 80
