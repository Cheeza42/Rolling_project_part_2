apiVersion: v1
kind: Service
metadata:
  name: awsviewer-metrics
  namespace: dev
  labels:
    app: awsviewer
spec:
  selector:
    app.kubernetes.io/name: awsviewer
  ports:
    - name: metrics
      port: 5001
      targetPort: 5001
